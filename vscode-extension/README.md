# Ash OS Search

A VS Code extension to search your Ash OS files semantically.

## Setup

1. Run `ash-api` on your machine (port 8080).
2. Install this extension.
3. Open command palette and run `Ash: Search Files`.
